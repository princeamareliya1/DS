#include <stdio.h>
#include <stdlib.h>
struct Student
{
    int roll_no; 
    char *name[100]; 
    char *branch[100];
 int batch_no;
};
int main()
{
    /*WAP to define a C structure named Student (roll_no, name, branch and
batch_no) and also to access the structure members using Pointer.*/
int n,i,j;
printf("enter n:");
scanf("%d",&n);

struct Student *a=(struct Student*)malloc(100*sizeof(struct Student));
printf("Enter Roll no:\n");
scanf("%d",&a->roll_no);
printf("enter name:\n");
scanf("%s",a->name);
printf("enter Branch:\n");
scanf("%s",a->branch);
printf("enter btch no:\n");
scanf("%d",&a->batch_no);
printf("%d\n",a->roll_no);
printf("%s\n",a->name);
printf("%s\n",a->branch);
printf("%d\n",a->batch_no);
free(a);
return 0;
}