#include <stdio.h>
#include<stdlib.h>
void main()
{int n,i;
    printf("Enter NO:");
    scanf("%d",&n);
    int *a = (int *)malloc(n*sizeof(int));
    for ( i = 0; i <n; i++)
    {
        printf("ENTER ELEMENT OF:");
        scanf("%d",&a[i]);
    }
      for ( i = 0; i <n; i++)
    {
       printf("%d\t",a[i]);
    }

    
    free(a);
}